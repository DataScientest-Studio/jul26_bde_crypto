# Dependances de l'API seulement : l'image Docker n'a pas besoin de MLflow,
# de Jupyter ni de matplotlib, qui pesent plusieurs centaines de Mo.
fastapi>=0.115
uvicorn[standard]>=0.30
pandas>=2.2
pyarrow>=15.0
numpy>=1.26
# Version FIGEE : le modele est serialise par scikit-learn, et une version
# differente dans l'image declenche un avertissement de compatibilite et,
# potentiellement, des predictions differentes de celles mesurees.
scikit-learn==1.9.0
ta>=0.11
joblib>=1.3
psycopg2-binary>=2.9
pymongo>=4.6
# Appels a l'API Binance pour les bougies en direct.
requests>=2.32
